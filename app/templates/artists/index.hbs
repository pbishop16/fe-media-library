<div class="artist-list">
  <ul>
    {{#each filteredArtists as |artist|}}
      {{#link-to 'artists.artist' artist}}<li class="artist-item">{{artist.name}}</li>{{/link-to}}
    {{/each}}
  </ul>
</div>
{{outlet}}
